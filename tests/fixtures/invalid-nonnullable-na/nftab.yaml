spec_version: "0.1.0"
dataset_id: "fixture-invalid-nonnullable"
storage_profile: "table-package"

observation_table:
  path: "observations.csv"
  format: "csv"

row_id: "row_id"
observation_axes: ["subject", "condition"]

observation_columns:
  row_id:
    dtype: string
    nullable: false
  subject:
    dtype: string
    nullable: false
  condition:
    dtype: string
    nullable: false
  group:
    dtype: string
    nullable: false
  roi_1:
    dtype: float32
    nullable: false

features:
  roi_beta:
    logical:
      kind: vector
      axes: ["roi"]
      dtype: float32
      shape: [1]
    encodings:
      - type: columns
        binding:
          columns: ["roi_1"]
