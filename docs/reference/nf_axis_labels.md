# Read axis labels for a feature axis

Read axis labels for a feature axis

## Usage

``` r
nf_axis_labels(x, feature, axis)
```

## Arguments

- x:

  An nftab object.

- feature:

  Feature name.

- axis:

  Axis name from the feature's logical schema.

## Value

A data.frame with at least `index` and `label`.
