# Logical schema for a feature

Logical schema for a feature

## Usage

``` r
nf_feature_schema(x, feature)
```

## Arguments

- x:

  An nftab object.

- feature:

  Feature name as a string or unquoted symbol.

## Value

An `nf_logical_schema` object.
