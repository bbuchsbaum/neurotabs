% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute.R
\name{nf_apply}
\alias{nf_apply}
\title{Apply a function or fixed operation to a feature row-by-row}
\usage{
nf_apply(x, feature, .f, ..., .progress = FALSE)
}
\arguments{
\item{x}{An \link{nftab} object.}

\item{feature}{Feature name.}

\item{.f}{Either a function or a character fixed operation.}

\item{...}{Additional arguments passed to \code{.f} when \code{.f} is a function.}

\item{.progress}{Show progress during generic row-wise resolution. Default
\code{FALSE}.}
}
\value{
A named vector or list with one result per row.
}
\description{
For character \code{.f} values in \code{c("mean", "sum", "sd", "min", "max", "nnz", "l2")}, \code{neurotabs} uses a fixed-operation path and will batch NIfTI reads
when possible. For a function \code{.f}, values are resolved row-by-row and passed
to that function.
}
\section{Parallelism}{

For NIfTI-backed features, file reads are automatically parallelized
using \code{\link[parallel:mclapply]{parallel::mclapply()}} with half the available cores. Set
\code{options(neurotabs.compute.workers = 1L)} to force sequential execution,
or a higher value for more parallelism. Disabled on Windows.
}

