% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{nf_ingest_parcel_csv}
\alias{nf_ingest_parcel_csv}
\title{Ingest a parcel-signal CSV into an nftab}
\usage{
nf_ingest_parcel_csv(
  path,
  design,
  parcel_cols = NULL,
  parcel_map = NULL,
  space = "unknown",
  feature = "parcel_signal",
  dataset_id = NULL
)
}
\arguments{
\item{path}{Path to the CSV file.  Every column not present in \code{design} (or
matched by \code{parcel_cols}) is treated as a parcel signal column.}

\item{design}{A data.frame with one row per observation.}

\item{parcel_cols}{Optional character vector of column names to use as parcel
signals.  If \code{NULL} (default), all CSV columns absent from \code{design} are
used.}

\item{parcel_map}{Optional path to a TSV (or data.frame) with columns
\code{index}, \code{label}, \code{n_voxels}, \code{x_centroid}, \code{y_centroid}, \code{z_centroid}.
When provided, an \code{\link[=nf_support_parcel]{nf_support_parcel()}} is attached.}

\item{space}{Named reference space.  Used only when \code{parcel_map} is
provided.  Default \code{"unknown"}.}

\item{feature}{Name to give the parcel-signal feature.  Default
\code{"parcel_signal"}.}

\item{dataset_id}{Dataset identifier for the manifest.}
}
\value{
An \link{nftab} object with a \code{columns} encoding (data inline).
}
\description{
Reads a CSV file where rows are observations and columns are parcel signals,
and wraps it into an \link{nftab} using a \code{columns} encoding (data stored inline
in the observation table).
}
