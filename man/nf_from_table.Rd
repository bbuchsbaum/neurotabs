% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{nf_from_table}
\alias{nf_from_table}
\title{Create an nftab from a table and external feature files}
\usage{
nf_from_table(
  observations,
  feature = "statmap",
  locator_col = NULL,
  locator = NULL,
  row_id = "row_id",
  axes = NULL,
  backend = NULL,
  space = NULL,
  dataset_id = "dataset",
  root = NULL
)
}
\arguments{
\item{observations}{A data.frame or path to a CSV/TSV file. Each row is one
observation with design metadata columns.}

\item{feature}{Name to give the feature (e.g. \code{"statmap"}).}

\item{locator_col}{Column name in \code{observations} containing per-row file
paths. Use this for the one-file-per-row pattern. Mutually exclusive with
\code{locator}.}

\item{locator}{A single file path shared by all rows (4D file). Row \code{i} maps
to volume \code{i-1} (0-based). Mutually exclusive with \code{locator_col}.}

\item{row_id}{Name of the row ID column. If the column does not exist, one
is generated automatically. Default \code{"row_id"}.}

\item{axes}{Character vector of observation axis column names. If \code{NULL},
auto-detected as all string columns except \code{row_id}, \code{locator_col}, and
any selector column.}

\item{backend}{Backend identifier. Default \code{NULL} (auto-detect from file
extension).}

\item{space}{Named reference space (e.g. \code{"MNI152NLin2009cAsym"}).}

\item{dataset_id}{Dataset identifier for the manifest.}

\item{root}{Base directory for resolving relative paths. If \code{NULL} and
\code{observations} is a file path, uses its parent directory.}
}
\value{
An \link{nftab} object.
}
\description{
The most common entry point for building an nftab from existing data.
Supports two patterns:
}
\details{
\itemize{
\item \strong{One file per row}: each row has a column pointing to a separate 3D file.
\item \strong{Shared file}: all rows map to successive volumes in a single 4D file.
}

The backend is inferred from file extension (\code{.nii}, \code{.nii.gz} -> \code{"nifti"})
or can be set explicitly.
}
