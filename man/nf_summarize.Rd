% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute.R
\name{nf_summarize}
\alias{nf_summarize}
\alias{nf_summarise}
\title{Summarize a feature across rows or groups}
\usage{
nf_summarize(x, feature, by = NULL, .f = "mean", ..., .progress = FALSE)

nf_summarise(x, feature, by = NULL, .f = "mean", ..., .progress = FALSE)
}
\arguments{
\item{x}{An \link{nftab} object.}

\item{feature}{Feature name as a string or unquoted symbol.}

\item{by}{Optional character vector of observation columns defining groups.}

\item{.f}{Either a function or a fixed reducer name (\code{"mean"}, \code{"sum"},
\code{"var"}, \code{"sd"}, \code{"se"}).}

\item{...}{Additional arguments passed to \code{.f} when \code{.f} is a function.}

\item{.progress}{Show progress during generic resolution. Default \code{FALSE}.}
}
\value{
If \code{by = NULL}, a single reduced value. Otherwise an \link{nftab} with one
row per group and a summarized feature named \code{feature}.
}
\description{
For character \code{.f} values in \code{c("mean", "sum", "var", "sd", "se")},
\code{neurotabs} performs an elementwise reduction over resolved feature values
and will batch NIfTI reads when possible. \code{"se"} computes the standard error
of the mean (\code{sd / sqrt(n)}). For a function \code{.f}, each group receives the
list of resolved values for that feature.
}
