% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{nf_analyze}
\alias{nf_analyze}
\title{Exploratory term tests over an NFTab feature}
\usage{
nf_analyze(
  x,
  feature,
  formula,
  se_feature = NULL,
  contrasts = "auto",
  .progress = FALSE
)
}
\arguments{
\item{x}{An \link{nftab} object.}

\item{feature}{Feature name to analyze.}

\item{formula}{Right-hand-side-only formula, for example \code{~ group * condition}
or \code{~ group * condition + (1 | subject)}.}

\item{se_feature}{Optional standard-error feature. Currently not supported.}

\item{contrasts}{\code{"auto"} or \code{list(auto_max_order = 1L/2L)}.}

\item{.progress}{Show progress while materializing feature values. Default
\code{FALSE}.}
}
\value{
An \link{nftab} with one row per generated test and feature maps/vectors
named \code{stat}, \code{p_value}, and \code{estimate} when a 1-df test has a signed
contrast estimate.
}
\description{
Fits fast exploratory term tests over a numeric NFTab feature and returns the
result as a new \link{nftab}. Rows in the output represent generated tests, while
result features hold the corresponding statistic maps or vectors.
}
\details{
This helper is intentionally narrow. It supports independent-row fixed
effects, plus a constrained repeated-measures mode via a single random term
of the form \code{(1 | subject)}. It is not a general mixed-model engine.
}
