% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-registry.R
\name{nf_register_backend}
\alias{nf_register_backend}
\title{Register a backend adapter}
\usage{
nf_register_backend(
  name,
  resolve_fn,
  detect_fn = NULL,
  write_fn = NULL,
  native_resolve_fn = NULL
)
}
\arguments{
\item{name}{Backend identifier (e.g., \code{"nifti"}, \code{"hdf5"}).}

\item{resolve_fn}{Function with signature
\verb{function(locator, selector, logical_schema)} returning the resolved
array/value.}

\item{detect_fn}{Optional function \verb{function(locator)} returning \code{TRUE}
if this backend can handle the given locator.}

\item{write_fn}{Optional function with signature
\verb{function(locator, value, logical_schema, template = NULL, source_ref = NULL)}
that materializes a derived feature value to \code{locator}.}

\item{native_resolve_fn}{Optional function with the same signature as
\code{resolve_fn} but returning a native R object (e.g., a \code{NeuroVol}) rather
than a plain array.  Used by \code{\link[=nf_resolve]{nf_resolve()}} when \code{as_array = FALSE}.}
}
\description{
Register a backend adapter
}
