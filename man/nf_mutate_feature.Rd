% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute.R
\name{nf_mutate_feature}
\alias{nf_mutate_feature}
\title{Mutate a derived feature}
\usage{
nf_mutate_feature(
  x,
  name,
  feature,
  .f,
  ...,
  logical = NULL,
  storage = c("auto", "columns", "nifti"),
  description = NULL,
  .progress = FALSE
)
}
\arguments{
\item{x}{An \link{nftab} object or \code{grouped_nftab}.}

\item{name}{Name of the derived feature to create.}

\item{feature}{Source feature name.}

\item{.f}{Function applied to each resolved feature value.}

\item{...}{Additional arguments passed to \code{.f}.}

\item{logical}{Optional \link{nf_logical_schema} describing the derived feature.
If omitted, the source logical schema is reused and outputs must conform to
it.}

\item{storage}{Storage strategy: \code{"auto"}, \code{"columns"}, or \code{"nifti"}.}

\item{description}{Optional description for the new feature.}

\item{.progress}{Show progress during generic row-wise resolution. Default
\code{FALSE}.

Rows where \code{.f} returns \code{NULL} are encoded as missing values, and the derived
feature is marked nullable.}
}
\value{
An updated \link{nftab} object, or \code{grouped_nftab} when input is grouped.
}
\description{
Applies \code{.f} row-by-row to an existing feature and materializes the result as
a new NFTab feature. By default, the derived feature preserves the source
logical schema and uses storage selected from that schema:
}
\details{
\itemize{
\item 1D features use \code{columns} encoding
\item volumetric features use temporary NIfTI resources
}
}
