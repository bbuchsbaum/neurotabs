% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grammar.R
\name{nf_sample}
\alias{nf_sample}
\title{Sample feature values at spatial coordinates across all observations}
\usage{
nf_sample(
  x,
  feature,
  coords,
  coord_type = c("voxel", "mm"),
  rows = NULL,
  .progress = FALSE
)
}
\arguments{
\item{x}{An \link{nftab} object.}

\item{feature}{Character name of a volumetric feature.}

\item{coords}{An \verb{n_coords x 3} integer matrix of voxel grid coordinates
(1-based) when \code{coord_type = "voxel"}, or an \verb{n_coords x 3} numeric
matrix of mm world coordinates when \code{coord_type = "mm"}.}

\item{coord_type}{\code{"voxel"} (default) or \code{"mm"}.  mm conversion requires
neuroim2 and native backend resolution.}

\item{rows}{Integer vector of row indices to include.  Defaults to all rows.}

\item{.progress}{Show progress messages every 10 rows?  Default \code{FALSE}.}
}
\value{
A numeric matrix with \code{length(rows)} rows and \code{nrow(coords)} columns.
Row names are set to the corresponding \code{row_id} values.
}
\description{
Extracts feature values at a fixed set of spatial coordinates for every
observation, returning an \verb{[n_obs x n_coords]} matrix.  This is the core
spatial query primitive — it maps directly to the \code{series_fun} contract
expected by tools like cluster.explorer.
}
