% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{nf_ingest_parcel_h5}
\alias{nf_ingest_parcel_h5}
\title{Ingest a parcellated fmristore HDF5 file into an nftab}
\usage{
nf_ingest_parcel_h5(
  path,
  design,
  scan_name = NULL,
  feature = "parcel_signal",
  dataset_id = NULL,
  space = "unknown",
  output_dir = NULL
)
}
\arguments{
\item{path}{Path to the HDF5 file.}

\item{design}{A data.frame with one row per observation (T rows).  Any
columns whose names match \code{"subject"}, \code{"session"}, \code{"run"}, or
\code{"condition"} are used as observation axes.}

\item{scan_name}{For multi-scan files: which scan to use.  Defaults to the
first scan.  Ignored for single-scan files.}

\item{feature}{Name to give the parcel-signal feature.  Default
\code{"parcel_signal"}.}

\item{dataset_id}{Dataset identifier for the manifest.  Derived from the
filename by default.}

\item{space}{Named reference space (e.g. \code{"MNI152NLin2009cAsym"}).
Default \code{"unknown"}.}

\item{output_dir}{Optional directory where the \code{parcel_map.tsv} will be
written.  If \code{NULL}, parcel metadata is embedded as in-memory only (not
written to disk).}
}
\value{
An \link{nftab} object with ref encodings pointing to \code{path}.
}
\description{
Builds an \link{nftab} from an \code{H5ParcellatedScanSummary}, \code{H5ParcellatedScan},
or \code{H5ParcellatedMultiScan} file.  One nftab row is created per observation
(timepoint / volume), with a ref encoding that reads a single row of the
\verb{T × K} summary matrix via the \code{"fmristore-parcel"} backend.
}
